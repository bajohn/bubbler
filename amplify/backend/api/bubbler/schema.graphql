type Restaurant @model {
  id: ID
  name: String
  description: String
  city: String
  neighborhood: String
}

type Circle @model {
  id: ID
  x: Int
  y: Int
}

type Query {
  bubblerhandler(msg: String): String
}