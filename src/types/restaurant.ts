export type Restaurant = {
    id? : string,
    name : string,
    description : string,
    city: string,
    neighborhood: string
  }; 

  export type Circle = {
    id? : string,
    x : number,
    y : number,
    mouseDown?: boolean
  }; 